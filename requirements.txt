# =====================================================================
# Heterogeneous Quant Backtesting Engine - Python Dependencies
# =====================================================================
# Python Version: >= 3.9, < 3.12 (pybind11 2.11+ 对 3.12 的 ABI 支持仍在完善)
# 编译工具链（非 pip 依赖，见 README）: g++ >= 9.4, CUDA >= 11.8, CMake >= 3.18
# ---------------------------------------------------------------------

# ---- C++/Python 桥接层 (Zero-copy via Buffer Protocol) ----
pybind11~=2.11.1

# ---- 数值计算核心 (SoA 内存对齐与向量化撮合) ----
numpy~=1.24.4

# ---- 数据处理与性能基准对标 ----
pandas~=2.0.3

# ---- 无头可视化渲染 (WSL/Server 环境下的 Agg 后端) ----
matplotlib~=3.7.2

# ---- 真实行情数据源 (主用) ----
yfinance~=0.2.40

# ---- 备选行情数据源 (A 股 Tushare Pro) ----
tushare~=1.2.89

# ---- 进度显示 (CUDA 数据上传 / 网格搜索迭代) ----
tqdm~=4.66.1

